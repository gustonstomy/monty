#include "monty.h"
/**
 * _queue - formst for stack
 * @head: pointer to pointer to stack.
 * @increament: element
 * Return: Nothing.
 */
void _queue(stack_t **head, unsigned int increament)
{
	(void)head, (void)increament;

	if (if_stack)
		if_stack = 0;
}
