#include "monty.h"
/**
 * v_queue - formst for stack
 * @head: pointer to pointer to stack.
 * @increament: element
 * Return: Nothing.
 */
void v_queue(stack_t **head, unsigned int increament)
{
	(void)head, (void)increament;

	if (if_head)
		if_head = 0;
}
