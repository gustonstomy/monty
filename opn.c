#include "monty.h"
/**
 * v_nop - nop doesn’t do anything.
 * @head: pointer to pointer in stack
 * @increament: element
 * Return: Nothing
 */
void v_nop(stack_t **head, unsigned int increament)
{
	(void)head;
	(void)increament;
}
