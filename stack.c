#include "monty.h"
/**
 * v_stack - sformat of stack.
 * @head: pointer to pointer of stack
 * @increadment: element
 * Return: Nothing.
 */
void v_stack(stack_t **head, unsigned int increadment)
{
	(void)head, (void)increadment;

	if (!if_stack)
		if_stack = 1;
}
